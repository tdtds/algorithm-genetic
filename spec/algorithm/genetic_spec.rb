require 'spec_helper'

describe Algorithm::Genetic do
  it 'has a version number' do
    expect(Algorithm::Genetic::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
